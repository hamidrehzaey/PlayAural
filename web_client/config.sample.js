// Copy to config.js for local or deployment-specific web client settings.
// config.js is intentionally not committed.
window.PLAYAURAL_WEB_CONFIG = {
  // Example for a local non-TLS development server:
  // serverUrl: "ws://127.0.0.1:8000",

  // Production default is used when this is empty.
  serverUrl: "",
};
